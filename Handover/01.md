# F.A.L.C.O.N. 開発進捗まとめ(2026/07/16時点)

## プロジェクト概要

- **正式名称**: F.A.L.C.O.N.(Fully Autonomous Linguistic Computing Operations Network)
- **意味**: 完全自律・言語処理・演算・操作・ネットワーク
- **頭脳**: Anthropic Claude API(Python経由で呼び出し)
- **リポジトリ**: https://github.com/Avuorina/FALCON
- **ライセンス**: MIT License
- **最終目標アーキテクチャ**: サーバー(Python/Claude頭脳をホスティング) + PWA(Progressive Web App)フロントエンド
  - PCでもスマホでも同じロジックを共有して使えるようにする
  - 現在はまず**PC単体で動くコンソール版**を作るところからスタート

---

## 現在のフォルダ構成

```
FALCON/
├── config/
├── core/
│   ├── brain.py           ← 完成(コード書き込み済み、未実行)
│   └── tools/
│       ├── weather.py       ← 完成(コード書き込み済み、未実行)
│       └── area_codes.json  ← 作成必要(下記参照)
├── logs/
├── tests/
├── .env                    ← 要APIキー記入
├── .env.example
├── .gitignore              ← .env が含まれているか要確認
├── LICENSE                 ← MIT
├── main.py                 ← 完成(コード書き込み済み、未実行)
├── README.md
└── requirements.txt         ← 要更新(下記参照)
```

---

## 未完了タスク(家に帰ってから最優先)

### 1. ライブラリのインストール(学校ネットワークでブロックされ未実施)

```powershell
cd C:\Work\Code\FALCON
python -m pip install anthropic python-dotenv requests
```

- 学校のネットワークで `pip install` が完全にタイムアウト(pypi.orgへの接続がブロックされている)
- ブラウザでは pypi.org は開けるが、pip自体の通信は弾かれている状態
- プロキシ設定は「直接アクセス(プロキシサーバーなし)」だったため、プロキシ経由の問題ではない
- **→ 家のネットワークで再試行すればおそらく解決するはず**

### 2. `.env` に実際のAPIキーを記入

```
ANTHROPIC_API_KEY=sk-ant-xxxxx...
```

- キー未発行なら https://console.anthropic.com/ で発行
- `.env.example` には空のままでOK(見本用)
- `.gitignore` に `.env` が入っているか要確認(入っていなければ追加)

### 3. `core/tools/area_codes.json` を新規作成

```json
{
  "瀬戸": "230010",
  "名古屋": "230010",
  "愛知": "230010",
  "静岡": "220010"
}
```

### 4. `requirements.txt` の中身を以下に更新

```
anthropic
python-dotenv
requests
```

---

## 動作確認の順番(家に帰ってから)

```powershell
cd C:\Work\Code\FALCON

# 1. ライブラリインストール
python -m pip install anthropic python-dotenv requests

# 2. 頭脳部分の動作確認
python core\brain.py

# 3. 天気取得ツールの動作確認
python core\tools\weather.py

# 4. FALCON全体(会話ループ)の動作確認
python main.py
```

---

## 完成済みコード

### `core/brain.py`

```python
import os
from dotenv import load_dotenv
from anthropic import Anthropic

# .envファイルの中身を読み込む
load_dotenv()

# APIキーを使ってClaudeのクライアント(接続窓口)を作る
client = Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

def ask_claude(user_message: str) -> str:
    """
    ユーザーのメッセージをClaudeに送って、返事を受け取る関数
    """
    response = client.messages.create(
        model="claude-sonnet-4-6",
        max_tokens=1000,
        messages=[
            {"role": "user", "content": user_message}
        ]
    )
    return response.content[0].text

# テスト実行(このファイルを直接実行した時だけ動く)
if __name__ == "__main__":
    reply = ask_claude("こんにちは、FALCON")
    print(reply)
```

### `core/tools/weather.py`

```python
import requests
import json
import os

# area_codes.jsonのファイルパス(このファイルから見た相対位置)
AREA_CODES_PATH = os.path.join(os.path.dirname(__file__), "area_codes.json")

# デフォルト地域(現在地の代わり、隼のよく行く場所)
DEFAULT_LOCATION = "瀬戸"


def get_area_code(location_name: str) -> str | None:
    """
    地名から気象庁の地域コードを検索する
    完全一致 → 部分一致の順で探す。見つからなければNoneを返す
    """
    with open(AREA_CODES_PATH, "r", encoding="utf-8") as f:
        area_data = json.load(f)

    # 完全一致でまず探す
    if location_name in area_data:
        return area_data[location_name]

    # 部分一致で探す(「名古屋市」→「名古屋」でヒットさせる)
    for key in area_data:
        if key in location_name or location_name in key:
            return area_data[key]

    return None


def get_weather(location: str = None, lat: float = None, lon: float = None) -> dict:
    """
    気象庁のデータから天気予報を取得する

    location: 地名(例: "名古屋")。指定なければデフォルト地域を使う
    lat, lon: 緯度経度(将来のスマホ対応用、現時点では未使用)
    """
    # 現時点ではlocationが無ければデフォルト地域を使う
    target_location = location if location else DEFAULT_LOCATION

    area_code = get_area_code(target_location)
    if area_code is None:
        return {"error": f"「{target_location}」の地域コードが見つかりませんでした。area_codes.jsonに追加してください。"}

    url = f"https://www.jma.go.jp/bosai/forecast/data/forecast/{area_code}.json"

    try:
        response = requests.get(url, timeout=10)
        response.raise_for_status()  # エラーがあれば例外を発生させる
        data = response.json()
    except requests.exceptions.RequestException as e:
        return {"error": f"天気データの取得に失敗しました: {e}"}

    # 気象庁JSONの構造から必要な情報を取り出す
    try:
        # data[0]が短期予報(今日・明日・明後日)のブロック
        time_series = data[0]["timeSeries"][0]
        area_name = time_series["areas"][0]["area"]["name"]
        weather_codes = time_series["areas"][0]["weathers"]  # 天気の説明文リスト
        dates = time_series["timeDefines"]  # 対応する日付リスト
    except (KeyError, IndexError) as e:
        return {"error": f"天気データの解析に失敗しました: {e}"}

    forecast = []
    for date, weather in zip(dates, weather_codes):
        forecast.append({"date": date, "weather": weather})

    return {
        "location": area_name,
        "forecast": forecast
    }


# テスト実行(このファイルを直接実行した時だけ動く)
if __name__ == "__main__":
    result = get_weather("名古屋")
    print(json.dumps(result, ensure_ascii=False, indent=2))
```

### `main.py`

```python
from core.brain import ask_claude

def main():
    print("FALCON起動しました。「終了」と入力すると終わります。")
    
    while True:
        user_input = input("隼: ")
        
        if user_input == "終了":
            print("FALCON: またな、隼。")
            break
        
        reply = ask_claude(user_input)
        print(f"FALCON: {reply}")


if __name__ == "__main__":
    main()
```

**注意**: `main.py` は `FALCON` フォルダ直下から実行すること(`core` フォルダの中からは実行しない)。

---

## 今後実装予定のツール(優先順位順)

1. **ドキュメント・メモ自動生成**(最優先、隼が一番欲しいと言っていた機能)
2. **タスク・スケジュール管理**
3. **天気取得**(設計完了、動作確認待ち)

---

## 技術的なメモ・注意点

- 気象庁APIは市区町村ピンポイントではなく地方単位の粒度(瀬戸市は名古屋と同じ「尾張地方」コード `230010` で代用)
- 「現在地」は今のPCスクリプト単体では取得不可 → デフォルト地域(瀬戸)で代用中。将来PWA化した際はスマホのGPSから緯度経度を受け取る設計に拡張予定(`get_weather`関数の`lat`/`lon`引数はそのための準備)
- APIキーは絶対に`.env`ファイルのみに記載し、Gitにコミットしないこと
- 学校のネットワークではpypi.orgへの直接通信(pip)がブロックされている。ブラウザでのアクセスは可能、プロキシ設定は「直接アクセス」。原因は学校側の意図的なファイアウォール制限と推測される